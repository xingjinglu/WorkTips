#include<iostream>
#include<vector>

using namespace std;

struct node{
  float val;
  struct node *next;
};

ll_vect merge_sort()
{
  
}

int merge(std::vector<node*> ll_vect)
{
  int num = ll_vect.size();
  

  return 0;
}

int main()
{
  std::vector<node*> ll_vect;
  merge(ll_vect);

}
